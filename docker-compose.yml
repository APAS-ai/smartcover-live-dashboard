version: "3.9"

services:
  dashboard:
    build:
      context: .
      dockerfile: docker/Dockerfile
    env_file:
      - .env
    ports:
      - "8501:8501"   # Streamlit Dashboard
      - "8080:8080"   # FastAPI Proxy API
    restart: unless-stopped
